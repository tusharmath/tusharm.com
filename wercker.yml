box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: wintersmith build
        code: ./node_modules/bin/wintersmith build
deploy:
  steps:
    - tusharm/gh-pages:
        token: $GH_TOKEN
        basedir: build