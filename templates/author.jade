mixin author(authorName)
	- var author = contents.authors[authorName + '.json']
	span.author
	if author
		if author.metadata.link
			a(href=author.metadata.link)= author.metadata.name
		else if author.metadata.email
			a(href='mailto:'+author.metadata.email)= author.metadata.name
	else
		=authorName
